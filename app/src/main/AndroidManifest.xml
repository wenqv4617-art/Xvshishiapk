<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

<!-- 申请真机振动、GPS定位、闹钟、通知监听、后台锁屏唤醒等安卓硬件权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- GPS 定位：网页侧“定位卡片/天气”功能所有版本都需要（Android 12+ 蓝牙扫描已由 BLUETOOTH_SCAN+neverForLocation 覆盖） -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    
    <!-- ★ 修复点：前台服务权限声明，Android 14+ 必须 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- ★ 保活与定时唤醒：精确闹钟 + 开机自启 -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- ★ 电池优化白名单：国产 ROM（小米/华为/OPPO/vivo/荣耀）下，不进白名单会在息屏后被
         系统清理，表现就是「离开 App 后微信 ClawBot 失联」。用户在面板上点一下会弹系统授权框。 -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    
    <!-- 新增：申请读写外部存储权限 (支持高低安卓版本兼容，打通物理存储扫歌与导出) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- 工作台工作区位于公共 Download：Android 11+ 需「所有文件访问」权限（用户在系统设置手动开启） -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" tools:ignore="ScopedStorage" />

    <!-- 蓝牙管理：读取已连接/已配对设备、SPP 串口控制、BLE 扫描与写入 -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <!-- neverForLocation：本应用不通过蓝牙扫描结果推导物理位置，Android 12+ 免定位即可扫描 -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="叙事诗小手机"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ★ 修复点：显式声明 foregroundServiceType="specialUse"，Android 14+ 启动前台服务的必要条件 -->
        <service
            android:name=".McpForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE" android:value="StoryPhone keepalive background chat polling service" />
        </service>

        <!-- ★ 后台保活心跳 Receiver：由 AlarmManager.setAndAllowWhileIdle 唤醒，Doze 下仍可触发，链式重排下一次 -->
        <receiver
            android:name=".BgPollReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- ★ 保活看门狗 Receiver：进程被国产 ROM 清掉后，由这条闹钟链把前台服务重新拉起来，
             是「离开 App 后微信 ClawBot 还能收到并能回」的最后一道保险。
             自己排下一次（自续期），用户点「停止接收」时才解除。 -->
        <receiver
            android:name=".KeepAliveReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.story.phone.ACTION_KEEPALIVE_TICK" />
            </intent-filter>
        </receiver>

        <!-- ★ 应用内定时闹钟 Receiver：到点发通知 + 振动 + 唤醒 WebView -->
        <receiver
            android:name=".InAppAlarmReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- ★ 开机自启 Receiver：开机后重启前台守护服务，恢复保活 -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- ★ 正在播放监听服务：解析媒体通知（歌名/歌手），需用户在系统设置开启"通知使用权" -->
        <service
            android:name=".NowPlayingListenerService"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- 说明：这里原本注册过一个「微信接入无障碍服务」，用于读取微信界面并代为回复。
             该方案已弃置（用无障碍读微信界面受版本与 ROM 差异影响大，且只能在微信处于
             前台时工作），现改为「App 内直连腾讯官方 iLink / ClawBot 协议」。
             服务类文件已删除，此处不再注册。 -->

    </application>

</manifest>
