# 2026-08-11 工作日志

## 第三轮：蓝牙报错修复 + 闹钟铃声循环 + AI 暂停放歌

### 蓝牙 Profile not supported 修复
- 根因：部分 ROM 未注册某 Profile 服务时 `BluetoothManager.getConnectedDevices(profile)` 抛 `IllegalArgumentException("Profile not supported: 2")`，异常冒泡导致整个设备列表读取失败。
- 修复：`BluetoothMcp.getDevicesJson` 逐 profile try-catch 并 Log.w 跳过，其余正常收集已连接 + 已配对设备。
- 用户澄清确认：每设备"注入"开关 = 只控制该设备信息是否进入 AI 提示词（localStorage mcp_bt_inject_{addr} → mcp_bluetooth_devices → app_prompts.js），符合现有实现，无需改动。

### 闹钟铃声循环播放直到关闭
- `playAlarmRingtoneNative` 去掉 20 秒自动停止 → MediaPlayer isLooping=true 无限循环；静态 `ringtonePlayer` 进程级持有。
- companion 新增 `stopRingtone()` / `isRingtonePlaying()`；onReceive 支持 ACTION_STOP_RINGTONE（通知"停止响铃"按钮 → addAction 系统图标 → requestCode 9993 → 停铃声+关通知）。
- AndroidMcp 桥接 `stopAlarmRingtone()` / `isAlarmRingtonePlaying()`；MCP 面板闹钟卡片加"停止闹钟铃声"按钮；JS cancelActiveAlarm 联动 stopAlarmRingtone（clearAlarmStatus 不停，保留响铃到用户操作）。

### AI 暂停/停止放歌
- 提示词核心交互指令一补充 `[STOP_MUSIC]`（与 [PLAY_MUSIC] 互斥）。
- app_chat.js 在线 + 离线两条解析路径都加 STOP_MUSIC 正则（/STOP_MUSIC|停止音乐|暂停音乐|MCP_STOP_MUSIC/）→ mcpSystem.stopMusic() → 原生 stopNativeMusic。

### 验证
- JS 三文件 node --check 通过；Kotlin 静态检查（ACTION_STOP_RINGTONE/stopRingtone/ringtonePlayer/addAction 引用一致）。
- 用户使用 GitHub Actions 自动打包，无需本地构建。

## 第二轮：闹钟修复 + 蓝牙管理板块 + 真实电量

### 闹钟不响根因与修复
- 根因1：`setAndroidSystemAlarm` 用 ACTION_SET_ALARM+SKIP_UI 在多数 OEM 上不可靠（JS 侧 systemAlarmOk 无条件置 true，假成功）。
- 根因2：InAppAlarmReceiver 用 setExactAndAllowWhileIdle（无 SCHEDULE_EXACT_ALARM 权限时降级 setAndAllowWhileIdle，Doze 下延迟）。
- 修复：schedule 首选 `AlarmManager.setAlarmClock`（无需权限、系统保证精确触发、状态栏显示闹钟图标），失败降级 exact/allowWhileIdle；onReceive 增加 Kotlin MediaPlayer 原生播放本地铃声（解析 ringtone 匹配 /Music/Storypoem，循环 20 秒，不依赖 JS）+ 三连振动；toast 文案修正。

### 新增蓝牙管理（真实控制）
- 新文件 `BluetoothMcp.kt`：getDevicesJson（已连接按 A2DP/HEADSET/HID/GATT profile 读取 + 已配对）、SPP 串口发送（SPP_UUID 00001101-...，后台线程 socket 保持连接）、BLE 扫描（8s 回调缓存）、BLE 特征值写（connectGatt+discoverServices+write，API33 兼容）、系统蓝牙开关（Android13+ 提示受限）、getBatteryStatusJson（BatteryManager 真实电量+充电状态）。
- AndroidMcp 桥接：bluetoothGetDevices/bluetoothSendSpp/bluetoothDisconnectSpp/bluetoothIsSppConnected/bluetoothSetEnabled/bluetoothScanBle/bluetoothGetBleResults/bluetoothBleWrite/bluetoothGetBleWriteResult/getBatteryStatus/openBluetoothSettings；releaseBluetoothIfHeld 由 Service onDestroy 调用。
- Manifest：BLUETOOTH(maxSdk30)/BLUETOOTH_ADMIN(maxSdk30)/BLUETOOTH_SCAN/BLUETOOTH_CONNECT；MainActivity 运行时权限加 API31+ 两项。
- index.html：定位天气卡片加 #mcp-battery-status 行；新增可折叠 details 蓝牙卡片（#mcp-bt-list 设备列表+每设备独立注入开关+SPP 发送框、#mcp-ble-list BLE 扫描+写特征值面板、添加设备按钮跳系统蓝牙设置）。
- app_chat_mcp.js：syncBattery/loadBluetoothDevices/renderBluetoothDevices/toggleBtInject(每设备 localStorage mcp_bt_inject_{addr})/syncBluetoothPromptData(存 mcp_bluetooth_devices)/sendSppToDevice/scanBle/renderBleDevices/bleWriteToDevice/handleBluetoothCommand([BLUETOOTH_CMD] 解析：info/send/disconnect/toggle/scan/ble_write)。
- app_prompts.js：注入电量（mcp_battery）+ 蓝牙设备（仅开启注入开关的，mcp_bluetooth_devices）+ 核心交互指令三·蓝牙控制语法。
- app_chat.js：新增 [BLUETOOTH_CMD] 正则解析（复用 mcpSystem.handleBluetoothCommand），擦除指令防污染气泡。
- openPanel 时自动刷新电量+蓝牙；syncLocation 联动 syncBattery。

### 验证
- JS 四文件 node --check 通过；index.html details 标签配对 2/2，新元素存在。
- Kotlin 未本地编译（无 JDK/gradle），需 Android Studio 构建。
- 真机测试要点：闹钟设 30 秒验证响铃振动；蓝牙需先授权"附近设备"，刷新看设备列表，SPP 发送测试串口设备，BLE 扫描+写测试智能硬件。

## 第一轮：修复 Hybrid 应用退出后 JS 中枢失效问题（主动发信 / 闹钟 / 桌宠）

### 根因
- 所有后台注入（心跳 BgPollReceiver、闹钟 InAppAlarmReceiver、桌宠双击）都通过 `AndroidMcp.mainActivity.findViewById(R.id.webview)` 找 Activity 的 WebView；用户退出应用 → Activity 与 WebView 销毁 → JS 引擎死亡，前台服务只保进程不保 JS。
- `MainActivity.onDestroy()` 主动调用 `stopBackgroundPolling()`，退出即取消心跳。
- `activeSessionId` 是纯内存变量，即使有第二个 JS 环境也无法恢复会话。

### 方案：Headless WebView 中枢（前台服务托管）
前台服务 `McpForegroundService` 内创建不挂窗口的隐藏 WebView 加载完整 `assets/index.html`，注册同一 `AndroidMCP` 桥接；所有 JS 注入目标改为 `AndroidMcp.getEffectiveWebView()`（中枢优先，Activity 兜底）。Activity 销毁后中枢由常驻保活通知保活。

### 改动文件
- `app/src/main/java/com/story/phone/AndroidMcp.kt`：单例化 `getInstance(context)`（applicationContext）；新增 `centerWebView` / `getEffectiveWebView()`；桌宠双击、下载进度回调改注入中枢。
- `app/src/main/java/com/story/phone/MainActivity.kt`：onCreate 用单例；**删除 onDestroy 里的 stopBackgroundPolling/unregisterMediaReceiver**；McpForegroundService 新增 `startHeadlessCenter()`（onPageFinished 注入 `initBackgroundCenter()`，onDestroy 销毁中枢）；BgPollReceiver / InAppAlarmReceiver 注入目标改 `getEffectiveWebView()`。
- `app/src/main/assets/app_chat.js`：`openWeChatDialog` 持久化 `mcp_active_session_id`；删除会话时移除。
- `app/src/main/assets/app_chat_mcp.js`：`_registerActiveAlarm` 持久化 `mcp_active_alarm_state`；`clearAlarmStatus`/`cancelActiveAlarm` 清除；文件末尾新增 `window.initBackgroundCenter()`（恢复会话与闹钟状态、标记 `__isBackgroundCenter`）。

### 防重
主动发信走 `db.desktop_pets` + localStorage 时间戳（`mcp_last_msg_time_{charId}`），双 WebView 共享 localStorage 天然防重。

### 验证
JS 三个文件通过 node --check。Kotlin 未本地编译（机器无 JDK/gradle wrapper），需用户在 Android Studio 构建。
真机验证要点：开启「设置-后台保活」开关（启动前台服务+常驻通知）→ 进会话 → 退出应用 → 观察：心跳日志、闹钟到点通知、桌宠冒泡/双击。
注意事项：厂商（小米/华为等）需允许自启动/后台运行白名单；强制停止应用会连前台服务一起杀，属系统限制。
