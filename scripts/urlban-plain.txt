# ============================================================
# URL 禁止列表（明文种子，仅供开发者维护）
# 每行一个 URL；# 开头为注释；重复项自动去重；非法 URL 自动跳过。
# 修改后执行：node scripts/urlban-tool.js build 本文件
# （默认输出到 app/src/main/assets/url_ban_list.js，内容将整体加密）
# ============================================================

# 示例条目（演示用，可随时删除/替换为真实需要禁止的站点）
https://blocked.example.com
