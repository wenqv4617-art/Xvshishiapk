pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "StoryPhone"
include(":app")
� App 内跑网易云 API）
        maven { url = uri("https://www.jitpack.io") }
    }
}
rootProject.name = "StoryPhone"
include(":app")
